# --- Required for the public, reproducible path (classic Bing or any compatible endpoint) ---
# If the reviewer has a Bing Web Search key, they can drop it here or set as an env var.
BING_SEARCH_KEY=
# Use ONE of these:
# 1) Global Bing API (works with consumer keys): https://api.bing.microsoft.com
# 2) Azure Cognitive Services Bing Search v7 endpoint (if they have that resource):
#    https://<your-resource>.cognitiveservices.azure.com
BING_SEARCH_ENDPOINT=

# --- Optional: Azure AI Foundry (Agents) + Grounding with Bing Search path ---
# Only if the reviewer wants to test via Azure AI Foundry Agents.
AZUREAI_PROJECT_ENDPOINT=        # e.g., https://aihub-xxx.services.ai.azure.com/api/projects/<projectName>
AZUREAI_PROJECT_API_KEY=         # Project-level API key (from Azure AI Foundry project)
AZUREAI_BING_CONNECTION_ID=      # e.g., /subscriptions/.../connections/<name>
AZUREAI_MODEL_DEPLOYMENT=        # e.g., gpt-4o-mini or your deployed chat model name
